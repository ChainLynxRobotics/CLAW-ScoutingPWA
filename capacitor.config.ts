import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chainlynx8248.scouting',
  appName: 'C.L.A.W. Scouting',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
